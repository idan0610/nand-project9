

class board{
	field int x;
    
    constructor board new(){
		let x = 1;
		do Screen.setColor(true);
		do Screen.drawCircle(200,200,5);
		do Screen.drawCircle(400,100,5);
    	do Screen.drawCircle(112,192,2);
		do Screen.drawCircle(320,144,10);
		
		
		return this;
    }
    
    //method void dispose(){
	//	do Memory.deAlloc(this);
	//	return;
	//	}
}
